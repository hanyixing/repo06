# 复制为 .env 后按需修改（.env 已被 .gitignore 忽略，请勿提交真实密钥）
# docker-compose 会自动读取本目录下的 .env 进行变量替换

# MySQL
# 注意：修改 MYSQL_ROOT_PASSWORD 时，需同步修改 application-docker.yml 中的 site.datasource_password
MYSQL_DATABASE=pybbs
MYSQL_ROOT_PASSWORD=root

# 应用运行参数
SPRING_PROFILES_ACTIVE=docker
SERVER_PORT=8080
