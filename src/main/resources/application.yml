server:
  port: 8080
spring:
  profiles:
    active: '@activatedProperties@'
  banner:
    location: ["classpath:banner.txt", "file:./banner.txt"]
  freemarker:
    suffix: .ftl
    cache: false
    settings:
      template_exception_handler: rethrow
      number_format: 0.##
    template-loader-path: ["classpath:/templates/", "file:./templates/"]
  messages:
    basename: i18n/message
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1


#  resources:
#    static-locations: ["classpath:static/", "file:./static/"]

# 配置mapper.xml位置，这个位置可以在pom.xml里配置
mybatis-plus:
  mapper-locations: classpath:/*Mapper.xml
  global-config:
    banner: false

# Actuator 健康检查：仅暴露 health 端点，供 Docker/compose 探活使用
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
